<script>
  import { exclude } from "utils/exclude";
  import Label from "./Label.svelte";
  let className = "";
  export let value = "";
  export let disabled = false;
  export let label = null;
  export { className as class };

  $: classes = `max-w-full border border-gray-500 px-2 py-1 rounded ${className}`;
</script>

{#if label}
  <Label {label} />
{/if}
<input class={classes} bind:value {disabled} {...exclude($$props, ['class'])} on:input on:blur on:click />
